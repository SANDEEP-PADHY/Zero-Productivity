{
  "name": "zero-productivity-browser-extension",
  "version": "1.0.0",
  "description": "Zero Productivity browser extension",
  "main": "dist/background.js",
  "scripts": {
    "build": "esbuild src/background.ts --bundle --outfile=dist/background.js --format=iife --target=es2022",
    "dev": "esbuild src/background.ts --bundle --outfile=dist/background.js --format=iife --target=es2022 --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "esbuild": "^0.20.1",
    "typescript": "^5.3.3"
  }
}
