# Dashboard Specification

Stack: Next.js + TypeScript.

Routes: `/`, `/timeline`, `/apps`, `/websites`, `/insights`, `/devices`, `/rules`, `/settings`, `/account`.

Overview answers: how much time, what consumed it, productivity distribution, and what changed.

Timeline supports date/device/app/site/classification/activity filters and windowed/paginated loading.
