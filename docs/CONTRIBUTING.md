# Contributing

Read relevant docs before coding. Use `feature/*`, `fix/*`, or `docs/*` branches. PRs should describe scope, affected platforms, tests, migrations, and privacy/security impact.

Schema changes require migrations. Production schema must never be changed manually without a migration.
